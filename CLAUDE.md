# LinkedIn AI

LinkedIn post generation and workflow automation. The repo currently holds writing instructions, Claude Code skills, and MCP configuration — no application code yet. If application code is added, use TypeScript and follow existing patterns.

## User Interests (for LinkedIn content discovery)
- AI / LLMs / AI Agents / AI Engineering
- Startups / Entrepreneurship / Fundraising / Growth

## Repository structure

```
.
├── sources/                      # LinkedIn writing guidance (canonical copies pulled from ClickUp)
│   ├── tone-of-voice.md          # Tone, taboos, sentence rhythm (Ukrainian)
│   └── post-instructions.md      # Post structure, mini-brief format, quality formula (Ukrainian)
├── dashboards/                   # LinkedIn analytics data + Grafana dashboard snapshots
│   ├── li-stats/                 # Raw LinkedIn analytics JSON (git-tracked, written by linkedin-stats agents)
│   └── grafana/                  # Exported Grafana dashboard JSONs (linkedin-stats.json + linkedin-stats-posts.json) — source-of-truth snapshots
├── linkedin-compain/             # Scheduled LinkedIn comment-ideas outputs (written by linkedin-comment-hourly, Tue–Fri morning fires)
│   └── comments.json             # SINGLE JSON array — one object per handled post, and the cross-fire seen-set. Every entry shares the same shape: {key, urn?, post_url?, author_url?, author_name, author_headline?, time_ago?, post_text, scraped_at, disposition, reason?, variants[], slack_summary, slack_ts, slack_thread, slack_error}. `post_url` is the post permalink — the verified canonical `https://www.linkedin.com/posts/<slug>/` URL when positive verification succeeded, else the raw captured link (`https://lnkd.in/p/<code>` short link, or the canonical `/posts/` URL the desktop copy-link now sometimes writes directly — tracking query stripped; both counted in `PERMALINKS_UNVERIFIED`), null only when capture failed entirely; `author_url` is the author's profile link. `disposition` ∈ drafted | off-topic | already-commented; drafted entries carry variants + Slack ts, filtered entries carry a `reason` + empty variants. `key` is the synthetic `<author-slug>-<body-hash8>` (LinkedIn's home-feed DOM has obfuscated CSS classes and no reliable data-urn as of 2026-07; body-hash is computed on a footer-cut body so mutable reaction counts / comment previews can't drift the key). Permalink recovery (accepted posts only; filtered cards carry `urn: null` / `post_url: null` — unverified link metadata is never handed out): the ONLY permalink source is the card's control-menu "Copy link to post" payload (an `lnkd.in` short link or, on some cards since 2026-07-24, the full canonical URL), captured by intercepting `navigator.clipboard.writeText` before clicking (clipboard `readText()` throws "Document is not focused" in the cron tab), resolved in a throwaway browser tab (`lnkd.in` serves a reCAPTCHA page to curl since 2026-07-16), and **positively verified** (author profile path or normalized body prefix must appear on the rendered page) → post_url = the verified final `/posts/<slug>/` URL (query stripped), urn = the type+id embedded in the slug (the thread's activity id). If verification can't confirm the page, the raw captured link is stored as post_url (short link as-is, canonical `/posts/` URL query-stripped) — never a rebuilt/guessed URL. `/feed/update/<urn>/` URLs are never emitted (they render unreliably outside the full web app — user-verified 2026-07-16) and URNs leaked in card HTML are ignored entirely (`activity` ids name the thread, `ugcPost`/`share` ids name the post — same post, different digits, wrong type 404s). Dedup = presence of `key` in the array, plus a fuzzy bridge (normalized author + body-prefix) that still matches entries whose keys were minted by the legacy bash hash recipe; repost/promoted cards are deliberately never written so a later original isn't suppressed.
├── doc/
│   ├── incidents/                # Self-heal incident reports (git-tracked) — one md per pipeline incident: diagnosis, assumptions, codex rounds, fix, follow-ups. Written by the heal sessions of BOTH scheduled pipelines (run-weekly.sh and run-hourly.sh); the durable knowledge channel between headless runs (they share no conversation memory)
│   └── context/                  # Conversational context ledgers (git-tracked) — requirements.md (incl. corrections), decisions.md, facts.md, follow-ups.md. Appended by the context archivist (see Conventions); the durable knowledge channel for what is said in conversation but never lands in a commit
├── prompts/                      # Ad-hoc prompt drafts (e.g., plan-mode prompts) — checked in for reuse, not consumed by Claude Code automatically
├── .claude/
│   ├── settings.json             # Permission allowlist + hooks config (UserPromptSubmit → history.py; Stop → history.py + backgrounded context.py sweep)
│   ├── hooks/                    # history.py — conversation-history capture; context.py + context-prompt.md + context-schema.json — context archivist (see Conventions)
│   ├── commands/                 # Slash commands: new-task.md (/new-task — roll the history file over)
│   ├── skills/                   # Project skills (see below)
│   └── agents/                   # Sub-agents spawned by skills via the Agent tool
├── .github/workflows/            # GitHub Actions — linkedin-stats-weekly + linkedin-comment-hourly, both on the self-hosted macOS runner
├── .github/scripts/              # CI helper scripts (build-stats-json.mjs: flattens li-stats/*.json into Pages-hosted stats.json for Grafana Infinity)
├── .mcp.json                     # MCP servers: context7, terminal, playwright, grafana, arxiv
├── start.sh                      # Local launcher: sources .env then execs `claude --dangerously-skip-permissions`
├── .env.example                  # Template for the gitignored .env that start.sh loads
└── CLAUDE.md                     # This file
```

`sources/` is the source-of-truth content for generating Peter's LinkedIn posts. It is overwritten by the `sync-sources` skill — local edits to those files are intentionally not preserved.

## Skills

Skills live in `.claude/skills/<name>/SKILL.md`. Multi-step skills with detailed sub-guidance keep that detail in `references/` next to the SKILL.md.

| Skill | Purpose |
|---|---|
| `common-pr-commit` | Branch + commit + push + update PR. Invokes `common-update-memory` before staging. |
| `common-pr-update` | Create or edit the PR with conventional-commit title and bullet body. |
| `common-pr-merge` | Squash-merge current branch's PR, clean up local + remote branches. |
| `common-update-memory` | Reviews staged diff and updates CLAUDE.md if structural/conceptual changes need to be reflected. Auto-called by `common-pr-commit`. |
| `sync-sources` | One-way pull of LinkedIn writing docs from ClickUp into `sources/`. |
| `awesome-sync-tasks` | Process `[AWESOME] Sync` Google Drive transcripts → create/update ClickUp tasks. |
| `weekly-priorities` | Process last week's meeting transcripts → update/create personal priorities in ClickUp. |
| `utilities-youtube-transcript` | Download a YouTube video's transcript via yt-dlp; falls back to Playwright agent on HTTP 429. Spawns `utilities-youtube-transcript-vtt` / `-playwright` sub-agents. |
| `linkedin-comment-ideas` | Generate 2-3 ready-to-paste LinkedIn comment variants in Petro's voice for a given post. Loads the post via Playwright (or accepts pasted text), runs a pre-work checklist against the Posted folder, Transcripts folder, ICP doc, and True BDD factsheet in Google Drive, then applies ONE of seven strategies per variant. Strategies live in `references/strategies.md`. |
| `linkedin-comment-hourly` | On every scheduled workflow fire: gather 5 feed posts that pass all filters (unseen + on-topic per `interests.md` + not already commented + not repost), invoke `linkedin-comment-ideas` per post (full pre-work checklist), append one entry per post to the single `./linkedin-compain/comments.json` array (drafted posts with variants; off-topic / already-commented posts with a `reason` + full post text so they double as the seen-set), and post each set of drafts to Slack channel `C0BF606R4N7`. **Fast gather first**: `fast/gather-feed.mjs` (colocated with the SKILL.md; playwright-core on the same `mcp-chrome-linkedin-ai` Chrome profile; deps via `npm install` in `fast/`, node_modules gitignored) runs the whole scrape deterministically in <5 min — ported control-menu card discovery + header/footer line stripping (footer cut keeps mutable reaction counts out of the body so keys don't drift), batched interest classification via ONE tool-free `claude -p` pinned-haiku call per 6 candidates (escalates to sonnet, then per-candidate singles; `interests.md` stays the no-code tuning knob), run-local `data-fg-id` card tagging for permalink recovery (**the only permalink source is the card's "Copy link to post"**: clipboard-writeText interception → in-browser short-link resolution → the final `/posts/<slug>/` URL, opened + positively verified — author-path/body-prefix match — before being emitted; `/feed/update/<urn>/` URLs are never emitted (they render unreliably outside the full web app, user-verified 2026-07-16) and leaked card URNs are ignored (`activity` ids name the thread, `ugcPost`/`share` ids name the post — guessing shipped 4 broken links); `lnkd.in` serves a reCAPTCHA page to curl since 2026-07-16 so resolution must be in-browser; filtered entries carry no link metadata), filtered-entry appends via `jq` (the only serializer that touches `comments.json`), and a KEY=VALUE contract in run-scoped `tmp/gather-feed/<ts>/` with post bodies as **files** (`POST_<i>_TEXT_FILE` — inline base64 blobs poisoned agent contexts and got generations refused). The colocated `fast/verify-links.mjs` re-opens every `POST_<i>_URL` from a contract in a real browser and asserts the post renders — the acceptance test for permalink drift. Keys are `<author-slug>-<body-hash8>` (node-native recipe; a fuzzy author+body-prefix secondary dedup bridges legacy bash-recipe keys). Exit codes: 0 ok / 10 partial / 20 auth / 21 profile-locked / 22 rate-limited / 23 fs / 30 selector-drift / 31 classifier-dead; ONLY 30 falls back to the legacy `linkedin-comment-hourly-gather-feed` agent. Drafting is **parallel**: a `prep-refs` sub-agent downloads the four reference sources ONCE per fire into a local cache at `$HOME/.cache/linkedin-ai-refs` (outside the worktree, re-fetching only Drive-modified docs), then all draft agents run concurrently reading only that local cache (zero GDrive) — only the Slack posts + `comments.json` writes stay sequential. Sub-agents: `linkedin-comment-hourly-gather-feed` (LEGACY fallback, Playwright MCP + interest classification), `linkedin-comment-hourly-prep-refs` (refreshes the local reference cache), and `linkedin-comment-hourly-draft` (cached-refs drafting, no GDrive; takes `POST_TEXT_FILE`, legacy `POST_TEXT_B64` still accepted). Driven Tue–Fri 05:28 UTC by `.github/workflows/linkedin-comment-hourly.yml` (self-hosted macOS runner) → colocated **self-healing** `run-hourly.sh` (built on `pipeline-shared/lib.sh`; re-execs itself from a detached `tmp/` copy so heal sessions may safely edit the tracked wrapper), which posts a 🟢 run-started Slack bookend to `C0BF606R4N7`, branches off `main`, then runs the fast gather under the shared attempt loop: up to 3 attempts (fresh `--out-dir` per attempt; `linkedin-compain/` reset to the committed baseline before each retry so a healed rerun verifies against clean state and the tree is never a cross-attempt hybrid), the contract validated beyond exit codes (numeric `POSTS_FOUND` + every `POST_<i>_TEXT_FILE` present — an ok exit with an unusable contract is a scraper bug and heals), at most ONE ≤30-min headless heal session per fire (`references/self-heal.md` overlay on `pipeline-shared/references/self-heal-core.md`; incident in `doc/incidents/<date>-linkedin-comment-hourly.md` + CLAUDE.md link). 20 auth / 22 rate-limit / 31 classifier-dead fail fast with NO heal (31 because the healer runs on the same `claude -p`); 21 sweeps once then heals on the second lock; 30 falls back to the legacy gather agent FIRST (drafts must ship) and runs a post-landing heal afterwards, flagged unverified-next-run; an accepted contract with `PERMALINKS_MISSING`>0 (an accepted post's copy-link capture failed end-to-end — the draft would ship without its post link; error since 2026-07-21) ships the drafts first, marks the fire ⚠️, and gets the same post-landing heal. Drafting: skipped entirely when `POSTS_FOUND=0` (still commits filtered appends), otherwise `claude -p --dangerously-skip-permissions` pointing at the accepted contract. Landing: unhealed fires chain `common-pr-commit` + `common-pr-update` (PR URL captured via `gh pr view "$BRANCH"` before the merge) + `common-pr-merge` as before (auto-merged PR, none left open); HEALED fires split — the `linkedin-compain/` data commit auto-merges on its own deterministic PR (Slack already received the drafts; an unmerged seen-set would make the next fire re-draft and re-post the same posts), while the heal's code + incident land on a `heal/…` PR left OPEN for review (same review-gate philosophy as the weekly pipeline). Finally a ✅/⚠️/❌ run-finished bookend — drafted/filtered counts, duration, PR URLs, heal status — from a self-disarming EXIT trap (+ TERM/INT traps) that covers every exit path. Bookends are best-effort pinned-haiku `claude -p` micro-calls (invocation shape now lives in `pipeline-shared/lib.sh` as `pl_post_slack`) with the message **inline in the prompt** (empirically pinned 2026-07-17: haiku refuses to post content handed on stdin "it can't see", and `--tools ""` strips MCP tools so the Slack connector vanishes and the model hallucinates the call as text — both silently posted nothing; `--allowedTools "mcp__claude_ai_Slack_Bot__postMessage"` + `--permission-mode dontAsk` pre-approve exactly the one tool and deny everything else, no `--tools`/`--setting-sources`; own 120s bash watchdog; `CLAUDE_HISTORY_ROLE=0`) — a Slack failure never fails the fire. Tune the filter by editing `interests.md` — no code change. |
| `linkedin-stats` | Snapshot Peter's LinkedIn posts + per-post + account-level weekly analytics into JSON files under `./dashboards/li-stats/` (git-tracked). **Fast path first**: `fast/scrape-weekly.mjs` (colocated with the SKILL.md; playwright-core on the same `mcp-chrome-linkedin-ai` Chrome profile; deps via `npm install` in `fast/`, node_modules gitignored) runs ALL four gather steps deterministically in ~5 min — line-anchored `main.innerText` parsing for the analytics surfaces (LinkedIn moved them to obfuscated CSS classes 2026-07, so label lines are the only stable anchors; the activity feeds + public post page kept semantic classes and reuse the canonical scrape .js files verbatim), per-endpoint-class nav pacing (demographic-detail slowest — it 429s first) with 429 cool-down + requeue, and every li-stats write routed through `fast/merge.py` (Python `json.dump` round-trips the corpus byte-for-byte where `JSON.stringify` rewrites historical float lexemes like `50.0`). Exit codes: 0 ok / 10 partial / 20 auth / 21 profile-locked / 22 rate-limited / 23 fs / 30 selector-drift (canary post failed to parse). Headroom for the unexplained 2026-07-20 runner-only nav slowdown: 90s goto timeout, 60s launch timeout with an orphan-Chrome sweep between attempts, and `page.goto` timeouts classify NETWORK→partial (exit 10, partials kept) instead of UNKNOWN→exit 1 (snapshot discarded). Semantic canaries in the account phase (followers>0, audience-demographic rows>0) demote hollow-but-parseable snapshots to exit 10 — the line-anchored parsers default missing anchors to zero/{}, which silently shipped six empty demographic groups on 2026-07-13/20. Agent flow (interactive fallback only — the CI path self-heals instead, see below): spawns `linkedin-stats-gather-posts` (URN discovery), then one `linkedin-stats-gather-metrics` agent **per post** sequentially (post-summary + 6 demographic breakdowns + top-level commenters from the public post URL → `weeks[WEEK].{metrics,demographics,comments}`; while on the public post page it also backfills the file's top-level `text` — the full post body — whenever it's null; gather-posts creates new files with `text: null`, and reposts stay null since metrics skips them), then `linkedin-stats-gather-account` (dashboard + 4 creator-analytics pages → `account.json`), then `linkedin-stats-gather-comments-out` (Peter's outbound comments from `/recent-activity/comments/`, URN-decoded for exact timestamps → `comments.json` keyed by `comments[urn]` with static metadata + `weeks[WEEK]` reactions/replies snapshots; discovery floor = oldest tracked post's `posted_date` with an incremental `RECENT_FLOOR_MS` shortcut on subsequent runs; weekly snapshot only for comments younger than 30 days at WEEK midnight UTC). Parallel fan-out across sub-agents is unsafe with the shared Playwright MCP (no per-call tab targeting). Driven weekly (Mon 00:00 UTC) by `.github/workflows/linkedin-stats-weekly.yml` via the colocated **self-healing** `run-weekly.sh` (built on `pipeline-shared/lib.sh`; re-execs itself from a detached `tmp/` copy so heal sessions may safely edit the tracked wrapper; posts 🟢 run-started and ✅/⚠️/❌ run-finished Slack bookends to `C0BF606R4N7` — attempts, heals, coverage, followers, PR URL — from the same self-disarming EXIT trap pattern as run-hourly.sh): up to 5 attempts of `node fast/scrape-weekly.mjs --deadline-secs=1500`, each under a hard kill-watchdog at deadline+600s (the mjs deadline is soft — a hung browser sails past it; no LLM on the happy path). Acceptable = exit 0, or exit 10 whose logged contract has **no phase-level `ERROR=` line** and ≥80% per-post coverage (`POSTS_MEASURED ≥ 4×(failed+unprocessed)`, measured>0 — a hollow partial is a failure wearing exit 10). Between failed attempts a watchdogged (75 min) headless `claude -p` heal session (GH_TOKEN scrubbed) follows `pipeline-shared/references/self-heal-core.md` + the `references/self-heal.md` overlay: diagnose with probes → codex validation round (`codex exec -s read-only --ephemeral`; GNU `timeout` doesn't exist on macOS — `perl -e 'alarm shift @ARGV; exec @ARGV'` is the portable cap) → triage → minimal fix (restraint scales with HEAL_COUNT — repeat sessions must not re-fix the same fingerprint; wrapper/workflow edits allowed but flagged "unverified next-run") → cheap spot-verify → incident write-up in `doc/incidents/<date>-linkedin-stats-weekly.md` (bash pre-creates the skeleton so a killed session still leaves a trace); `$HEAL_DIR/ABORT` stops the loop (auth wall etc.). No-heal exceptions: exit 22 sleeps 20 min once, a second consecutive 429 stops the loop (may be account-level — don't poke); exit 21 sweeps the orphan Chrome once, a second consecutive lock goes to a heal session. Before each retry `dashboards/li-stats/` is reset to the committed baseline (never commit a cross-attempt hybrid). After the loop: `jq empty`-validate snapshots (clean path: invalid = hard fail; heal path: corrupt file reverted so the incident/fix commit survives). **Auto-merge only a no-heal exit-0 run** (`common-pr-commit`+`-update`+`-merge`, sets the `main_updated=true` step output that gates the Pages `publish` job — healed/partial runs leave main stale, so publish skips). Everything else — recovered, accepted-partial, aborted, exhausted — appends the attempt table to the incident, inserts a link under CLAUDE.md `## Incidents`, runs a **read-only** critique session on recovered successes (`pipeline-shared/references/self-heal-review-core.md` + the `references/self-heal-review.md` overlay, codex round 2; no code edits — the committed code must be the code that passed the rerun), then commit + PR **without merge** for Peter's review. Recovered/partial runs exit green (the open PR is the review surface); aborted/exhausted exit red. Watchdog subtlety: the killer subshell's own `sleep` must be pkill'd by parent PID before the subshell, or the orphaned sleep holds the step's stdout pipe open. The legacy multi-hour agent fallback is gone from CI (recover from git history if ever needed); heal claude gets `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0` so its background codex runs survive headless claude's 600s ceiling. |

## Dashboard

LinkedIn analytics are visualised in two Grafana Cloud dashboards on `https://boarlabs.grafana.net`. The data is collected by the `linkedin-stats` skill into JSON under `dashboards/li-stats/`, flattened to a single payload by `.github/scripts/build-stats-json.mjs`, and published to GitHub Pages as the sole file at `https://boarlabsxyz.github.io/linkedin-ai/stats.json`. Grafana reads it via the Infinity datasource (`grafanacloud-infinity`).

Two workflows publish the file (both share the `pages` concurrency group, both require Pages source = **GitHub Actions** in repo settings):
- `.github/workflows/linkedin-stats-weekly.yml` — Mon 00:00 UTC. Two jobs: `scrape` runs on the self-hosted Mac and ends after `run-weekly.sh` lands the new JSONs on `main`; `publish` (`needs: scrape`) runs on `ubuntu-latest`, checks out fresh `main`, builds `stats.json`, uploads via `actions/upload-pages-artifact`, deploys via `actions/deploy-pages`. The Mac runner no longer runs `actions/upload-pages-artifact` (which requires `gtar`). When a run self-healed or accepted a partial, its data sits on an unmerged PR and `publish` is skipped (gated on the scrape job's `main_updated` output); merge the PR, then dispatch `pages-deploy.yml` to get the week onto the dashboard.
- `.github/workflows/pages-deploy.yml` — manual `workflow_dispatch`: republishes `stats.json` from current `main` without scraping.

The two Grafana dashboards:
- **`linkedin-stats`** (uid `kiqz2fk`, `/d/kiqz2fk/linkedin-stats`) — Account view: KPIs, trends, audience demographics, posts-per-month, comments-per-month (count + reactions + impressions), plus a full-width Plotly scatter (`ae3e-plotly-panel`, requires plugin install) of "posts published that month" (X) vs "post's latest-snapshot impressions" (Y) that fits a quadratic curve via in-browser Gaussian elimination on the normal equations (R² in the legend). No variables. `posts_per_month` entries include `total_impressions` (sum of each post's latest-snapshot `impressions`) and `avg_impressions_per_post`; both pull from the latest weekly snapshot, so the trailing month reads low until posts accumulate impressions. `correlation_points` (one entry per post: `posts_in_month`, `impressions`, `id`, `posted_date`, `type`) feeds the scatter panel; `correlation_trend` in `stats.json` (2 endpoints of an OLS line) is now unused — the panel fits its own curve in-browser. A second full-width Plotly panel ("Impressions per post over time") plots the same per-post points with `posted_date` on X and overlays an IRLS median (L1) regression line plus a 200-resample percentile bootstrap 95% band (deterministic LCG seeded by N so the band doesn't shimmer on refresh) — the robust alternative the cadence research recommended for heavy-tailed creator data, with pseudo-R² (1 − Σ\|y−ŷ\| / Σ\|y−median\|) in the legend.
- **`LinkedIn Stats — Per-post`** (uid `linkedin-post`, `/d/linkedin-post/...`) — Per-post view: `$post` Custom variable picker, selected-post table (full post body from `posts.text` with wrapped cells + a clickable `post_url` column — an Infinity query, not the old variable-label markdown panel), 4 weekly bar charts (Impressions / Engagement actions / Engagement rate / Profile viewers & followers gained), weekly metrics table, 6 audience demographic bars. Per-post charts cap at the first 12 weekly snapshots via `sortBy(week asc) + limit 12`.

Source-of-truth JSONs for both dashboards live in `dashboards/grafana/`. Dashboards are built/updated via the `mcp__grafana__*` MCP tools (no UI clicks); the original research spec lives in `prompts/grafana-linkedin-stats-replica.md`. Notable Infinity gotchas: backend-mode queries ignore the `filters:[]` array — use `filterExpression: "id == \"${post}\""` and include every filtered field in `columns`. Variable resolution requires Custom variable type with `query` in `display : value, display : value, …` format (the Query variable type silently fails to populate options in Grafana 12 v0alpha1 dashboards). Date strings break `==` parsing — use `contains(week, "2026-06")` instead of `week == "2026-06-01"`.

## External systems

- **ClickUp** — source of truth for the LinkedIn writing docs (workspace `90151491867`), for AWESOME tasks (list `901522119783` in space `901510520225`), and for personal priorities (list `901522189872`). Skill files contain the specific IDs.
- **Google Drive** — meeting transcripts. AWESOME single transcripts folder: `14I2yIWsoZ5BTJD-Sqk9nVkU23iC11eYJ`.
- **Google Calendar** — used by `weekly-priorities` to scope the previous week's meetings.
- **MCP servers** (in `.mcp.json`): `context7` (library docs), `terminal` (interactive terminal), `playwright` (browser automation), `grafana` (Grafana Cloud — `https://boarlabs.grafana.net`; reads `GRAFANA_SERVICE_ACCOUNT_TOKEN` from the launching shell's env), `arxiv` (arXiv paper search/download via `uvx arxiv-mcp-server`; paper storage under `tmp/arxiv`).

## Local launch

Launch Claude Code via `./start.sh` — it sources the gitignored `.env` (env vars referenced by `.mcp.json`, e.g. `GRAFANA_SERVICE_ACCOUNT_TOKEN`) and execs `claude --dangerously-skip-permissions`. On first checkout, `cp .env.example .env` and fill in the values. Additional CLI args pass through (`./start.sh /some-skill`).

## Scheduled jobs (GitHub Actions on the self-hosted runner)

Both recurring jobs run as GitHub Actions crons on the self-hosted macOS runner (next section). GH cron fires can run late by 0–2h+ (observed on this repo) — acceptable at daily/weekly cadence (hourly GH crons drop >75%, see [community#196910](https://github.com/orgs/community/discussions/196910), which is why the now-retired launchd path existed):

- `.github/workflows/linkedin-stats-weekly.yml` — Mon 00:00 UTC weekly stats scrape + Pages publish (see Dashboard). Self-healing: failed scrape attempts trigger headless heal sessions that fix the scraper, log an incident to `doc/incidents/`, and leave the PR unmerged for review (details in the `linkedin-stats` skill row). Job cap `timeout-minutes: 1200` (20h — was 6000): a run wedged OUTSIDE the in-script watchdogs must not occupy the single runner past Tuesday 05:28, because GitHub silently cancels self-hosted jobs queued >24h and the hourly fire would vanish without a bookend.
- `.github/workflows/linkedin-comment-hourly.yml` — Tue–Fri 05:28 UTC (07:28 Podgorica summer / 06:28 winter; off-hour minute because the on-the-hour 04:00 slot got dropped by GitHub's scheduler on 2026-07-21) comment-drafts pipeline: one `draft` job runs `.claude/skills/linkedin-comment-hourly/run-hourly.sh` under `timeout-minutes: 180`, the outer backstop above the script's own watchdogs (420s per gather attempt, one ≤30-min heal session, 40-min drafting `CLAUDE_TIMEOUT_SECS=2400`) — the in-script watchdogs must win because they still commit partial drafts (after a `jq empty` truncation check), land the heal branch, and sweep the orphaned `mcp-chrome-linkedin-ai` browser, while a runner-level kill loses all of that and gives the EXIT-trap Slack bookend only ~10s to post (vs launchd's 60s `ExitTimeOut`). `concurrency: linkedin-comment-hourly` (no cancel-in-progress) queues a late cron fire behind a manual dispatch; overlap with the weekly scrape needs no guard — the single runner processes one job at a time, which also serializes the shared Chrome profile. Fire on demand: `gh workflow run linkedin-comment-hourly.yml`.

The launchd LaunchAgent path was retired 2026-07-20 (label `xyz.boarlabs.slack-heartbeat`; `scripts/cron-wrapper.sh` + `scripts/slack-heartbeat.sh` + plist + installer; runtime at `~/cron_workers/linkedin-ai/`) — recover the scripts from git history if a schedule ever needs to outlive the Actions runner. The LaunchAgent must stay booted out (`launchctl bootout gui/$(id -u)/xyz.boarlabs.slack-heartbeat` + plist removed from `~/Library/LaunchAgents/`) or it would double-fire against the workflow at 06:00 CEST; `~/cron_workers/linkedin-ai/` is inert leftovers, safe to delete.

## Self-hosted runner (PetersMacStudio)

Workflows tagged `runs-on: [self-hosted, macOS]` execute on Peter's Mac Studio via the actions/runner LaunchAgent at `~/Library/LaunchAgents/actions.runner.boarlabsxyz.PetersMacStudio.plist`. Runner install dir: `/Users/peterovchinnikov/github_runner/actions-linkedin-ai/`. The runner reads Claude Code's OAuth from the login keychain (`Claude Code-credentials`), which carries the `user:mcp_servers` scope — so `claude -p` sees the same claude.ai account-level connectors (Slack, Gmail, GDrive, ClickUp, HubSpot, …) that interactive Claude sees, without any `.mcp.json` bot-token duplication. **Do NOT pass `CLAUDE_CODE_OAUTH_TOKEN` to `claude -p` steps** — the setup-token env is inference-only and strips connectors ([anthropics/claude-code#62556](https://github.com/anthropics/claude-code/issues/62556)). The plist must have `<key>SessionCreate</key><false/>` (default stock value is `true`, which puts the process in its own security session and blocks login-keychain reads → `Not logged in`). After editing the plist, restart via `./svc.sh stop` in the runner dir. LaunchAgents die on logout, so Peter must stay logged in on the Mac Studio (auto-login OK; login keychain stays unlocked via `security show-keychain-info login.keychain-db` → `no-timeout`).

## Conventions

- **Skill scope:** if a workflow has more than ~50 lines of detail, split sub-guidance into `references/` files next to the SKILL.md. Keep SKILL.md focused on flow + constants.
- **Bundled scripts in skills:** PR skills (`common-pr-commit`, `common-pr-merge`, `common-pr-update`) keep their bash logic in colocated `.sh` files (e.g., `commit.sh`, `merge.sh`, `pr-update.sh`), allowlisted by path in `.claude/settings.json` to avoid permission prompts on compound-bash parsing. `commit.sh` and `pr-update.sh` invoke `claude -p` internally to generate the commit message and PR copy from the diff.
- **`*-shared/` skill folders:** folders under `.claude/skills/` with a `-shared` suffix (e.g., `utilities-shared/`, `pipeline-shared/`) hold scripts referenced by other skills rather than being invocable skills themselves — no `SKILL.md`, just shared utilities. `pipeline-shared/lib.sh` is the Template-Method skeleton both scheduled-pipeline drivers source (bash 3.2 — the runner's only bash): hardened watchdog spawn/await, the narrow Chrome-profile sweep, the pinned Slack micro-call (`pl_post_slack`), the heal/review claude runner, incident plumbing, and the `pl_attempt_loop` template whose Strategy hooks (`pipeline_run_attempt` / `pipeline_classify` / `pipeline_reset_baseline` / `pipeline_heal_prompt`) each driver defines — hooks are called directly and communicate via `PL_*` globals, never `$(…)` (a subshell would silently lose counter state). `pipeline-shared/references/` holds the IMMUTABLE self-heal / review core protocols; each pipeline adds an additive overlay under its own `references/` (exit taxonomy, budgets, known history), and heal sessions may never edit `pipeline-shared/` itself.
- **Sub-agents:** thin orchestrator skills can spawn sub-agents from `.claude/agents/<name>.md` via the Agent tool. Each agent's frontmatter declares its `tools:` allowlist and `model:`. Examples: `utilities-youtube-transcript` spawns `utilities-youtube-transcript-vtt` and `utilities-youtube-transcript-playwright`; `linkedin-stats` spawns `linkedin-stats-gather-posts`, `linkedin-stats-gather-metrics`, `linkedin-stats-gather-account`, and `linkedin-stats-gather-comments-out`; `linkedin-comment-hourly` spawns `linkedin-comment-hourly-prep-refs` (refreshes the local reference cache once per fire, incremental by Drive modified-date) and `linkedin-comment-hourly-draft` (parallel per-post wrapper around `linkedin-comment-ideas` in cached-refs mode — reads only the local cache, no GDrive tools, so many run concurrently); `linkedin-comment-hourly-gather-feed` (Playwright home-feed scrape + interest classification) is now only the selector-drift fallback for the deterministic `fast/gather-feed.mjs`.
- **Transcript language:** AWESOME Sync and weekly meeting transcripts are in Russian/Ukrainian; ClickUp output is always in English with consistent transliteration (e.g., always "Petro", not sometimes "Peter").
- **Temp files:** use `./tmp/` and clean up afterward. Listed in `.gitignore`.
- **ClickUp writes need validation:** AWESOME and weekly-priorities both validate every extracted item one-by-one before writing — auto-generated tasks/priorities are noisy and require human judgment.
- **Conversation-history hooks:** `.claude/hooks/history.py prompt-submit` is wired to BOTH UserPromptSubmit and Stop (same command; mirrors `awesome-claude-mcp`). It appends every turn to `./tmp/history/<UTC-ts>-<session8>-<slug>.md` (gitignored): `## user` per prompt, `## claude` per finished assistant turn (all text blocks since the last prompt, read from the transcript JSONL; the payload's `last_assistant_message` backstops an unflushed tail). Each heading carries a UTC timestamp + short git sha. Tool calls are intentionally skipped. `./tmp/history/hook-state` holds the active file's name and is shared across sessions — a new session continues the same file; `/new-task` (`.claude/commands/new-task.md`) deletes the state file so the next prompt opens a fresh one (its own prompt + ack are filtered out). Headless `claude -p` workers set `CLAUDE_HISTORY_ROLE` (branch-name / commit-msg in `common-pr-commit/commit.sh`, pr-content in `common-pr-update/pr-update.sh`) so their prompts log as `## claude to @<role>`; `CLAUDE_HISTORY_ROLE=0` disables logging entirely; sub-agent prompts (payload `agent_id`) are skipped. Legacy transcripts from the retired shell-hook system remain under `doc/history/` (also gitignored) but nothing writes there anymore.
- **Context archivist:** `.claude/hooks/context.py sweep` distills task transcripts into the `doc/context/` ledgers — after EVERY response, not just at task boundaries. The Stop hook chains it behind `history.py` (backgrounded, so the turn never waits): each sweep finalizes any finished history files, then incrementally processes the ACTIVE file's newest chunk — a byte offset in `tmp/history/context-processed/<file>.offset` tracks what's been distilled, and growth under ~300 bytes is skipped — so ledgers stay current mid-task; `/new-task` still triggers the finalizing sweep after rolling the state file over. Per chunk, ONE `codex exec -s read-only --ephemeral` call (schema-forced JSON via `--output-schema` + `-o`; prompt in `.claude/hooks/context-prompt.md` — the no-code tuning knob) extracts requirement / decision / correction / fact / follow_up items that pass three admission tests (a future session would act differently; not recoverable from code, git history, CLAUDE.md, or an existing ledger; human-confirmed or empirically verified) — all-empty is the expected result for routine turns. Merge-with-override: an item may carry a `supersedes` substring naming the older ledger bullet it contradicts — the script strikes the old line through (`~~…~~ _(superseded DATE)_`, audit trail kept, never deleted) and appends the new one; consecutive chunks of one task append into a single dated section instead of stacking headings. The script (the only writer of `doc/context/`) files corrections into requirements.md with a `[correction]` prefix and writes the raw reply as the done-marker to `tmp/history/context-processed/<file>.json` — markers/offsets advance only on success, so failed codex runs retry next sweep; an flock on `tmp/history/context-sweep.lock` prevents double-appends; sweeps no-op under `GITHUB_ACTIONS` / `CLAUDE_HISTORY_ROLE` so pipeline runs and headless `claude -p` workers never burn codex calls; log at `tmp/history/context-sweep.log`. Ledger promotion into CLAUDE.md prose happens through the normal `common-update-memory` path when ledger changes get committed — the archivist never edits CLAUDE.md.

## Git workflow rules

- **Never** create branches, switch branches, commit, push, or create PRs unless explicitly requested in the user's prompt, plan, or instructions.
- Git operations are user-initiated only — do not proactively perform any git actions.
- **When the user says "commit", "commit changes", "push", or any variation** — always use the `common-pr-commit` skill via the Skill tool. Do NOT follow manual git commit steps.

## Context

Durable conversational knowledge — requirements, decisions, corrections, facts, follow-ups that never land in commits — lives in the `doc/context/` ledgers, extracted from task transcripts by the context archivist (see Conventions): [requirements](doc/context/requirements.md) · [decisions](doc/context/decisions.md) · [facts](doc/context/facts.md) · [follow-ups](doc/context/follow-ups.md). Check the relevant ledger before working in an area it covers.

## Incidents

- 2026-07-20 — weekly scrape: transient empty activity feed + audience demographics empty under concurrent load; recovered after 2 attempts / 1 heal (account phase serialized before metrics, adaptive per-tab poll), PR review pending — [doc/incidents/2026-07-20-linkedin-stats-weekly.md](doc/incidents/2026-07-20-linkedin-stats-weekly.md)
- 2026-07-24 — hourly comments: one accepted draft shipped to Slack without its post link (`PERMALINKS_MISSING=1`); root cause "Connect" header chrome leaking into the parsed body (broke the verify body-prefix) combined with the desktop copy-link writing a full canonical URL that the lnkd.in-only unverified-keep dropped silently; post-landing heal fixed parser + keep-chain + reason-coded verify logging, repaired 14 seen-set entries, heal PR review pending — [doc/incidents/2026-07-24-linkedin-comment-hourly.md](doc/incidents/2026-07-24-linkedin-comment-hourly.md)
